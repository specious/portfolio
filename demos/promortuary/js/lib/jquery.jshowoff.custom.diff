--- jquery.jshowoff.js	2011-09-24 16:03:59.000000000 -0400
+++ jquery.jshowoff.custom.js	2011-09-24 16:05:43.000000000 -0400
@@ -232,8 +232,12 @@
 					$('<a class="jshowoff-slidelink-'+i+' '+uniqueClass+'-slidelink-'+i+'" href="#null">'+linktext+'</a>').bind('click', {index:i}, function(e){ goToAndPause(e.data.index); return false; }).appendTo('.'+uniqueClass+'-slidelinks');
 				});
 			};		
-	
-	
+      // The following was brutally hacked in just for Lum :D
+      $('.menu-item').each(
+        function( i ) {
+          $(this).bind( "click mouseenter", { idx: i }, function( e ) { goToAndPause( e.data.idx ); } );
+        }
+      );
 		// end .each
 		});
 	
